# for a new project:-
#frst make a folder and then add that project in that folder, then in that folder write:-

git init
git add .
git commit -m "frst commit"  any msg
git remote add origin <link of repo>
git push origin master


#For some changes in an existing repo
git commit -m "second commit"
git push origin master


*****AND IF YOU PUSHED A CODE AND WANT TO UPDATE ON GITHUB THEN***********
go to that project folder:-
	git add .
	git commit -m "any message" 
	git push origin master